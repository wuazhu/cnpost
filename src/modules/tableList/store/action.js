// import * as constants from './constant'
import services from '../../../conf/services'
import http from '../../../http'

// 执行登陆操作
export async function getTabList({ commit }, data) {
  // let res = http.$crm.post(services.getTabList, data)
  console.log(123)
}
